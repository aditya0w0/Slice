@extends('layouts.app')

@section('title', 'Messages - Slice')

@section('content')
<div class="h-screen bg-[#0B0C10] flex overflow-hidden font-sans selection:bg-blue-500/30 selection:text-blue-100">
    
    <!-- Chat Container -->
    <div class="flex-1 flex flex-col bg-[#0B0C10]">
        
        <!-- Chat Header -->
        <div class="h-16 px-6 bg-[#0F1015] border-b border-white/5 flex items-center justify-between">
            <div class="flex items-center gap-4">
                <a href="{{ route('dashboard') }}" class="p-2 text-slate-400 hover:text-white hover:bg-white/5 rounded-lg transition-colors">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
                    </svg>
                </a>
                
                <div class="flex items-center gap-3">
                    <img src="{{ $activeChat['user']['avatar'] }}" class="w-10 h-10 rounded-full border-2 border-white/10" alt="">
                    <div>
                        <h3 class="text-sm font-semibold text-white">{{ $activeChat['user']['name'] }}</h3>
                        <p class="text-xs text-green-500 flex items-center gap-1">
                            <span class="w-2 h-2 bg-green-500 rounded-full"></span>
                            {{ $activeChat['user']['status'] }}
                        </p>
                    </div>
                </div>
            </div>
            
            <div class="flex items-center gap-2">
                <button class="p-2 text-slate-400 hover:text-white hover:bg-white/5 rounded-lg transition-colors">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 5v.01M12 12v.01M12 19v.01M12 6a1 1 0 110-2 1 1 0 010 2zm0 7a1 1 0 110-2 1 1 0 010 2zm0 7a1 1 0 110-2 1 1 0 010 2z" />
                    </svg>
                </button>
            </div>
        </div>

        <!-- Messages Area -->
        <div id="messages-container" class="flex-1 overflow-y-auto p-6 space-y-4" style="background: linear-gradient(180deg, #0B0C10 0%, #0F1015 100%);">
            @if(count($activeChat['messages']) === 0)
            <!-- Empty State -->
            <div class="flex flex-col items-center justify-center h-full text-center">
                <div class="w-20 h-20 bg-white/5 rounded-full flex items-center justify-center mb-4">
                    <svg class="w-10 h-10 text-slate-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z" />
                    </svg>
                </div>
                <h3 class="text-lg font-semibold text-white mb-2">Start a conversation</h3>
                <p class="text-sm text-slate-500 max-w-sm">Send a message to our support team. We're here to help with any questions or issues you may have.</p>
            </div>
            @else
            <!-- Date Separator -->
            <div class="flex justify-center my-4">
                <span class="px-3 py-1 bg-white/5 text-slate-500 text-xs rounded-full border border-white/10">Today</span>
            </div>

            @foreach($activeChat['messages'] as $message)
            <!-- Message -->
            <div class="flex {{ $message['sender'] === 'me' ? 'justify-end' : 'justify-start' }} group animate-fade-in-up">
                <!-- Avatar if 'them' -->
                @if($message['sender'] === 'them')
                <img src="{{ $activeChat['user']['avatar'] }}" class="w-8 h-8 rounded-full mr-3 mt-1 border-2 border-[#0B0C10] self-end mb-1">
                @endif

                <div class="max-w-[70%]">
                    <!-- Bubble -->
                    <div class="relative px-5 py-3 rounded-2xl text-sm leading-relaxed shadow-md
                        {{ $message['sender'] === 'me'
                            ? 'bg-blue-600 text-white rounded-br-sm'
                            : 'bg-[#1F2029] border border-white/5 text-slate-200 rounded-bl-sm'
                        }}">
                        {{ $message['content'] }}
                    </div>

                    <!-- Meta -->
                    <div class="flex items-center gap-1.5 mt-1 {{ $message['sender'] === 'me' ? 'justify-end' : 'justify-start' }} opacity-0 group-hover:opacity-100 transition-opacity duration-200">
                        <p class="text-[10px] text-slate-500">{{ $message['time'] }}</p>
                        @if($message['sender'] === 'me')
                            <svg class="w-3 h-3 text-blue-500" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" /></svg>
                        @endif
                    </div>
                </div>

                <!-- Avatar if 'me' -->
                @if($message['sender'] === 'me')
                <img src="{{ Auth::user()->profile_photo ? asset('storage/' . Auth::user()->profile_photo) : 'https://ui-avatars.com/api/?name=' . urlencode(Auth::user()->name) . '&background=0D8ABC&color=fff' }}" class="w-8 h-8 rounded-full ml-3 mt-1 border-2 border-[#0B0C10] self-end mb-1">
                @endif
            </div>
            @endforeach
            @endif
        </div>

        <!-- Input Zone -->
        <div class="p-5 bg-[#0B0C10] border-t border-white/5">
            <form id="message-form" onsubmit="sendMessage(event)" class="relative">
                <div class="relative flex items-center bg-[#16171D] border border-white/10 rounded-xl shadow-lg focus-within:border-blue-500/50 focus-within:ring-1 focus-within:ring-blue-500/50 transition-all">
                    <!-- Attachment -->
                    <button type="button" class="p-3 text-slate-500 hover:text-white transition-colors">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.172 7l-6.586 6.586a2 2 0 102.828 2.828l6.414-6.586a4 4 0 00-5.656-5.656l-6.415 6.585a6 6 0 108.486 8.486L20.5 13" /></svg>
                    </button>

                    <input
                        type="text"
                        id="message-input"
                        placeholder="Type a message to support..."
                        class="flex-1 bg-transparent border-none text-sm text-white placeholder-slate-500 focus:ring-0 py-3.5 px-2"
                        autocomplete="off"
                    >

                    <!-- Actions -->
                    <div class="flex items-center gap-2 pr-2">
                        <button type="button" class="p-2 text-slate-500 hover:text-white transition-colors">
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.828 14.828a4 4 0 01-5.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
                        </button>
                        <button type="submit" class="p-2 bg-blue-600 hover:bg-blue-500 text-white rounded-lg transition-all shadow-lg shadow-blue-600/20 hover:scale-105">
                            <svg class="w-5 h-5 transform rotate-90 ml-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8" /></svg>
                        </button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>

<style>
@keyframes fade-in-up {
    from {
        opacity: 0;
        transform: translateY(10px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.animate-fade-in-up {
    animation: fade-in-up 0.3s ease-out;
}
</style>

<script>
    function sendMessage(event) {
        event.preventDefault();

        const messageInput = document.getElementById('message-input');
        const message = messageInput.value.trim();

        if (!message) return;

        fetch('{{ route("chat.send") }}', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
                'X-CSRF-TOKEN': '{{ csrf_token() }}'
            },
            body: JSON.stringify({
                message: message
            })
        })
        .then(response => response.json())
        .then(data => {
            if (data.success) {
                const messagesContainer = document.getElementById('messages-container');
                const messageDiv = document.createElement('div');
                messageDiv.className = 'flex justify-end group animate-fade-in-up';
                messageDiv.innerHTML = `
                    <div class="max-w-[70%]">
                        <div class="relative px-5 py-3 rounded-2xl text-sm leading-relaxed shadow-md bg-blue-600 text-white rounded-br-sm">
                            ${message}
                        </div>
                        <div class="flex items-center gap-1.5 mt-1 justify-end opacity-0 group-hover:opacity-100 transition-opacity duration-200">
                            <p class="text-[10px] text-slate-500">Just now</p>
                            <svg class="w-3 h-3 text-blue-500" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" /></svg>
                        </div>
                    </div>
                    <img src="{{ Auth::user()->profile_photo ? asset('storage/' . Auth::user()->profile_photo) : 'https://ui-avatars.com/api/?name=' . urlencode(Auth::user()->name) . '&background=0D8ABC&color=fff' }}" class="w-8 h-8 rounded-full ml-3 mt-1 border-2 border-[#0B0C10] self-end mb-1">
                `;
                messagesContainer.appendChild(messageDiv);
                messagesContainer.scrollTop = messagesContainer.scrollHeight;
                messageInput.value = '';
            }
        })
        .catch(error => {
            console.error('Error:', error);
            alert('Failed to send message. Please try again.');
        });
    }

    // Auto-scroll to bottom on load
    document.addEventListener('DOMContentLoaded', function() {
        const messagesContainer = document.getElementById('messages-container');
        if (messagesContainer) {
            messagesContainer.scrollTop = messagesContainer.scrollHeight;
        }
    });
</script>
@endsection

    <!-- Main Chat Area -->
    <div class="flex-1 flex flex-col bg-white">
        <!-- Chat Header -->
        <div class="px-6 py-4 border-b border-gray-200 bg-white">
            <div class="flex items-center justify-between">
                <div class="flex items-center gap-3">
                    <div class="relative">
                        <img src="{{ $activeChat['user']['avatar'] }}" alt="User" class="w-10 h-10 rounded-full object-cover">
                        <div class="absolute bottom-0 right-0 w-3 h-3 bg-green-500 rounded-full border-2 border-white"></div>
                    </div>
                    <div>
                        <h3 class="text-base font-semibold text-gray-900">{{ $activeChat['user']['name'] }}</h3>
                        <p class="text-sm text-green-600">{{ $activeChat['user']['status'] }}</p>
                    </div>
                </div>
                <div class="flex items-center gap-2">
                    <button class="p-2 text-gray-500 hover:text-gray-700 hover:bg-gray-100 rounded-lg transition-colors">
                        <i class="fas fa-search"></i>
                    </button>
                    <button class="p-2 text-gray-500 hover:text-gray-700 hover:bg-gray-100 rounded-lg transition-colors">
                        <i class="fas fa-ellipsis-v"></i>
                    </button>
                </div>
            </div>
        </div>

        <!-- Messages Area -->
        <div class="flex-1 overflow-y-auto p-6 bg-gray-50">
            <div class="space-y-4">
                <!-- Date Separator -->
                <div class="flex justify-center my-4">
                    <span class="px-3 py-1 bg-gray-200 text-gray-600 text-xs rounded-full">Today</span>
                </div>

                @foreach($activeChat['messages'] as $message)
                <div class="flex {{ $message['sender'] === 'me' ? 'justify-end' : 'justify-start' }}">
                    <div class="flex items-end gap-2 max-w-[70%] {{ $message['sender'] === 'me' ? 'flex-row-reverse' : '' }}">
                        @if($message['sender'] === 'them')
                        <img src="{{ $activeChat['user']['avatar'] }}" class="w-8 h-8 rounded-full">
                        @endif

                        <div>
                            @if($message['type'] === 'text')
                            <div class="px-4 py-2 rounded-2xl {{ $message['sender'] === 'me' ? 'bg-blue-600 text-white' : 'bg-white text-gray-900 border border-gray-200' }}">
                                <p class="text-sm">{{ $message['content'] }}</p>
                            </div>
                            @elseif($message['type'] === 'audio')
                            <div class="px-4 py-3 rounded-2xl bg-white border border-gray-200 flex items-center gap-3 min-w-[280px]">
                                <button class="w-10 h-10 bg-blue-600 hover:bg-blue-700 rounded-full flex items-center justify-center text-white">
                                    <i class="fas fa-play text-xs ml-1"></i>
                                </button>
                                <div class="flex-1 h-8 flex items-center gap-0.5">
                                    @for($i = 0; $i < 30; $i++)
                                    <div class="w-1 bg-gray-300 rounded-full" style="height: {{ rand(20, 100) }}%"></div>
                                    @endfor
                                </div>
                                <span class="text-xs text-gray-500">{{ $message['duration'] }}</span>
                            </div>
                            @elseif($message['type'] === 'card')
                            <div class="bg-white border border-gray-200 rounded-2xl p-4 space-y-3">
                                <p class="text-sm text-gray-900">{{ $message['content'] }}</p>
                                <div class="grid grid-cols-3 gap-2">
                                    <div class="bg-gray-50 rounded-xl p-3 text-center">
                                        <p class="text-xs text-gray-500 uppercase">Visitors</p>
                                        <p class="text-lg font-bold text-gray-900">{{ $message['card_data']['daily_visitors'] }}</p>
                                    </div>
                                    <div class="bg-gray-50 rounded-xl p-3 text-center">
                                        <p class="text-xs text-gray-500 uppercase">Age</p>
                                        <p class="text-lg font-bold text-gray-900">{{ $message['card_data']['building_age'] }}</p>
                                    </div>
                                    <div class="bg-gray-50 rounded-xl p-3 text-center">
                                        <p class="text-xs text-gray-500 uppercase">Temp</p>
                                        <p class="text-lg font-bold text-gray-900">{{ $message['card_data']['temperature'] }}</p>
                                    </div>
                                </div>
                            </div>
                            @elseif($message['type'] === 'gallery')
                            <div class="bg-white border border-gray-200 rounded-2xl p-3 space-y-2">
                                <p class="text-sm text-gray-900">{{ $message['content'] }}</p>
                                <div class="flex gap-2">
                                    @foreach($message['images'] as $index => $img)
                                    <div class="relative w-24 h-24 rounded-lg overflow-hidden">
                                        <img src="{{ $img }}" class="w-full h-full object-cover">
                                        @if($index === 2)
                                        <div class="absolute inset-0 bg-black/60 flex items-center justify-center">
                                            <span class="text-lg font-bold text-white">3+</span>
                                        </div>
                                        @endif
                                    </div>
                                    @endforeach
                                </div>
                            </div>
                            @endif

                            <p class="text-xs text-gray-500 mt-1 {{ $message['sender'] === 'me' ? 'text-right' : '' }}">
                                {{ $message['time'] }}
                            </p>
                        </div>

                        @if($message['sender'] === 'me')
                        <img src="https://i.pravatar.cc/150?u=me" class="w-8 h-8 rounded-full">
                        @endif
                    </div>
                </div>
                @endforeach
            </div>
        </div>

        <!-- Input Area -->
        <div class="p-4 bg-white border-t border-gray-200">
            <div class="flex items-center gap-3 bg-gray-50 rounded-lg px-4 py-2 border border-gray-200">
                <button class="text-gray-500 hover:text-gray-700 transition-colors">
                    <i class="fas fa-paperclip text-lg"></i>
                </button>
                <input 
                    type="text" 
                    placeholder="Type a message..." 
                    class="flex-1 bg-transparent focus:outline-none text-sm text-gray-900 placeholder-gray-500"
                >
                <button class="text-gray-500 hover:text-gray-700 transition-colors">
                    <i class="fas fa-smile text-lg"></i>
                </button>
                <button class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg transition-colors">
                    <i class="fas fa-paper-plane"></i>
                </button>
            </div>
        </div>
    </div>
</div>
@endsection

    <!-- Ambient Background -->
    <div class="absolute top-0 right-0 w-[800px] h-[600px] bg-blue-600/5 rounded-full blur-[120px] pointer-events-none"></div>
    <div class="absolute bottom-0 left-0 w-[600px] h-[600px] bg-teal-500/5 rounded-full blur-[100px] pointer-events-none"></div>

    <!-- Main Container -->
    <div class="w-full max-w-[1600px] h-full bg-[#121217]/80 backdrop-blur-2xl border border-white/5 rounded-3xl shadow-2xl overflow-hidden flex relative z-10">

        <!-- 1. Ultra-Slim Navigation Rail -->
        <div class="w-20 flex flex-col items-center py-6 border-r border-white/5 bg-[#0B0C10]/50">
            <!-- Workspace Logo -->
            <div class="w-10 h-10 rounded-xl bg-gradient-to-br from-blue-600 to-teal-500 flex items-center justify-center text-white shadow-lg shadow-blue-500/20 mb-8 hover:scale-105 transition-transform cursor-pointer">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
                    <path d="M13 10V3L4 14h7v7l9-11h-7z"/>
                </svg>
            </div>

            <!-- Nav Items -->
            <div class="space-y-4 flex flex-col items-center w-full">
                <button class="group relative w-10 h-10 rounded-xl flex items-center justify-center text-slate-400 hover:bg-white/10 hover:text-white transition-all">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path></svg>
                    <div class="absolute left-0 top-1/2 -translate-y-1/2 w-1 h-0 bg-blue-500 rounded-r-full transition-all group-hover:h-5"></div>
                </button>
                <button class="group relative w-10 h-10 rounded-xl bg-white/10 text-white flex items-center justify-center shadow-inner">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 10h.01M12 10h.01M16 10h.01M9 16H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-5l-5 5v-5z"></path></svg>
                    <div class="absolute left-0 top-1/2 -translate-y-1/2 w-1 h-5 bg-blue-500 rounded-r-full"></div>
                </button>
                <button class="group relative w-10 h-10 rounded-xl flex items-center justify-center text-slate-400 hover:bg-white/10 hover:text-white transition-all">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path></svg>
                </button>
            </div>

            <div class="flex-grow"></div>

            <!-- Bottom Actions -->
            <div class="space-y-4 flex flex-col items-center w-full mb-4">
                <button class="w-10 h-10 rounded-xl flex items-center justify-center text-slate-400 hover:bg-white/10 hover:text-white transition-all">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"></path><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path></svg>
                </button>
                <div class="w-8 h-8 rounded-lg bg-slate-800 border border-white/10 flex items-center justify-center text-xs font-bold text-white ring-2 ring-transparent hover:ring-blue-500/50 transition-all cursor-pointer">
                    N
                </div>
            </div>
        </div>

        <!-- 2. Contact Sidebar -->
        <div class="w-80 bg-[#16171D]/50 border-r border-white/5 flex flex-col">
            <!-- Search -->
            <div class="p-5 border-b border-white/5">
                <h2 class="text-xl font-bold text-white mb-4">Inbox</h2>
                <div class="relative group">
                    <input type="text" placeholder="Filter messages..." class="w-full bg-[#0B0C10] border border-white/10 rounded-xl py-2.5 pl-10 pr-4 text-sm text-white placeholder-slate-500 focus:border-blue-500/50 focus:ring-1 focus:ring-blue-500/50 transition-all outline-none">
                    <svg class="w-4 h-4 text-slate-500 absolute left-3.5 top-3 group-focus-within:text-blue-400 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path></svg>
                </div>
            </div>

            <!-- Contact List -->
            <div class="flex-1 overflow-y-auto p-3 space-y-1 custom-scrollbar">
                @foreach($contacts as $contact)
                <div class="group flex items-center p-3 rounded-xl cursor-pointer transition-all {{ isset($contact['active']) ? 'bg-white/10 border border-white/5 shadow-lg shadow-black/20' : 'hover:bg-white/5 border border-transparent' }}">
                    <!-- Avatar -->
                    <div class="relative">
                        <img src="{{ $contact['avatar'] }}" alt="{{ $contact['name'] }}" class="w-10 h-10 rounded-full object-cover ring-2 {{ isset($contact['active']) ? 'ring-white/20' : 'ring-transparent' }}">
                        @if($contact['status'] === 'online')
                        <div class="absolute bottom-0 right-0 w-2.5 h-2.5 bg-green-500 rounded-full border-2 border-[#16171D] shadow-[0_0_8px_rgba(34,197,94,0.6)]"></div>
                        @endif
                    </div>

                    <!-- Text Info -->
                    <div class="ml-3 flex-1 min-w-0">
                        <div class="flex justify-between items-baseline">
                            <h3 class="text-sm font-semibold text-white truncate {{ isset($contact['active']) ? 'text-blue-100' : '' }}">{{ $contact['name'] }}</h3>
                            <span class="text-[10px] font-medium {{ isset($contact['active']) ? 'text-slate-300' : 'text-slate-600 group-hover:text-slate-500' }}">{{ $contact['time'] }}</span>
                        </div>
                        <p class="text-xs truncate mt-0.5 {{ $contact['is_typing'] ? 'text-green-400 font-medium' : (isset($contact['active']) ? 'text-slate-300' : 'text-slate-500 group-hover:text-slate-400') }}">
                            {{ $contact['is_typing'] ? 'Typing...' : $contact['last_message'] }}
                        </p>
                    </div>

                    <!-- Badge -->
                    @if($contact['unread'] > 0)
                    <div class="ml-2 min-w-[18px] h-[18px] bg-blue-500 rounded-full flex items-center justify-center text-[10px] font-bold text-white shadow-lg shadow-blue-500/30">
                        {{ $contact['unread'] }}
                    </div>
                    @endif
                </div>
                @endforeach
            </div>
        </div>

        <!-- 3. Chat Area -->
        <div class="flex-1 flex flex-col bg-[#0B0C10]/30 relative">

            <!-- Chat Header -->
            <div class="h-16 flex items-center justify-between px-6 border-b border-white/5 bg-[#121217]/50 backdrop-blur-md z-20">
                <div class="flex items-center">
                    <div class="relative">
                        <img src="{{ $activeChat['user']['avatar'] }}" alt="User" class="w-9 h-9 rounded-full object-cover ring-2 ring-white/10">
                        <div class="absolute bottom-0 right-0 w-2.5 h-2.5 bg-green-500 rounded-full border-2 border-[#121217] shadow-[0_0_8px_rgba(34,197,94,0.6)]"></div>
                    </div>
                    <div class="ml-3">
                        <h3 class="text-sm font-bold text-white flex items-center gap-2">
                            {{ $activeChat['user']['name'] }}
                            <span class="px-1.5 py-0.5 rounded bg-white/5 border border-white/5 text-[10px] text-slate-400 font-normal">Support Lead</span>
                        </h3>
                        <p class="text-[10px] text-green-400 flex items-center font-medium">
                            {{ $activeChat['user']['status'] }}
                        </p>
                    </div>
                </div>
                <div class="flex items-center space-x-3">
                    <button class="w-8 h-8 rounded-lg flex items-center justify-center text-slate-400 hover:bg-white/10 hover:text-white transition-colors">
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path></svg>
                    </button>
                    <button class="w-8 h-8 rounded-lg flex items-center justify-center text-slate-400 hover:bg-white/10 hover:text-white transition-colors">
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 5v.01M12 12v.01M12 19v.01M12 6a1 1 0 110-2 1 1 0 010 2zm0 7a1 1 0 110-2 1 1 0 010 2zm0 7a1 1 0 110-2 1 1 0 010 2z"></path></svg>
                    </button>
                </div>
            </div>

            <!-- Messages Canvas -->
            <div class="flex-1 overflow-y-auto p-6 space-y-6 custom-scrollbar">
                <!-- Date Separator -->
                <div class="flex justify-center">
                    <span class="px-3 py-1 rounded-full bg-white/5 border border-white/5 text-[10px] font-medium text-slate-500">Today</span>
                </div>

                @foreach($activeChat['messages'] as $message)
                    <div class="flex {{ $message['sender'] === 'me' ? 'justify-end' : 'justify-start' }} group animate-fade-in-up">
                        <!-- Avatar Left -->
                        @if($message['sender'] === 'them')
                        <img src="{{ $activeChat['user']['avatar'] }}" class="w-8 h-8 rounded-full mr-3 mt-1 opacity-80">
                        @endif

                        <div class="max-w-[65%] relative">
                            @if($message['type'] === 'text')
                                <div class="p-3.5 px-5 rounded-2xl text-sm leading-relaxed shadow-md {{ $message['sender'] === 'me' ? 'bg-gradient-to-br from-blue-600 to-blue-700 text-white rounded-tr-sm' : 'bg-[#1F2029] border border-white/5 text-slate-200 rounded-tl-sm' }}">
                                    {{ $message['content'] }}
                                </div>

                            @elseif($message['type'] === 'audio')
                                <div class="p-3 rounded-2xl bg-[#1F2029] border border-white/5 shadow-sm flex items-center space-x-3 min-w-[280px]">
                                    <button class="w-8 h-8 bg-blue-600 hover:bg-blue-500 rounded-full flex items-center justify-center text-white transition-colors shadow-lg shadow-blue-500/20">
                                        <svg class="w-3.5 h-3.5 ml-0.5" fill="currentColor" viewBox="0 0 24 24"><path d="M8 5v14l11-7z"/></svg>
                                    </button>
                                    <div class="flex-1 h-8 flex items-center space-x-0.5 opacity-60">
                                        @for($i = 0; $i < 30; $i++)
                                            <div class="w-1 bg-slate-400 rounded-full transition-all hover:bg-blue-400 hover:h-full" style="height: {{ rand(20, 100) }}%"></div>
                                        @endfor
                                    </div>
                                    <span class="text-xs font-medium text-slate-400">{{ $message['duration'] }}</span>
                                </div>

                            @elseif($message['type'] === 'card')
                                <div class="bg-[#1F2029] border border-white/5 rounded-2xl rounded-tl-sm p-4 space-y-4 shadow-lg max-w-md">
                                    <p class="text-sm text-slate-200 font-medium">{{ $message['content'] }}</p>
                                    <div class="grid grid-cols-3 gap-2">
                                        <!-- Stat 1 -->
                                        <div class="bg-white/5 rounded-xl p-3 border border-white/5 hover:bg-white/10 transition-colors">
                                            <div class="w-8 h-8 rounded-lg bg-blue-500/10 flex items-center justify-center mb-2">
                                                <svg class="w-4 h-4 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path></svg>
                                            </div>
                                            <p class="text-[10px] uppercase tracking-wider text-slate-500">Visitors</p>
                                            <p class="text-sm font-bold text-white">{{ $message['card_data']['daily_visitors'] }}</p>
                                        </div>
                                        <!-- Stat 2 -->
                                        <div class="bg-white/5 rounded-xl p-3 border border-white/5 hover:bg-white/10 transition-colors">
                                            <div class="w-8 h-8 rounded-lg bg-purple-500/10 flex items-center justify-center mb-2">
                                                <svg class="w-4 h-4 text-purple-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path></svg>
                                            </div>
                                            <p class="text-[10px] uppercase tracking-wider text-slate-500">Age</p>
                                            <p class="text-sm font-bold text-white">{{ $message['card_data']['building_age'] }}</p>
                                        </div>
                                        <!-- Stat 3 -->
                                        <div class="bg-white/5 rounded-xl p-3 border border-white/5 hover:bg-white/10 transition-colors">
                                            <div class="w-8 h-8 rounded-lg bg-orange-500/10 flex items-center justify-center mb-2">
                                                <svg class="w-4 h-4 text-orange-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path></svg>
                                            </div>
                                            <p class="text-[10px] uppercase tracking-wider text-slate-500">Temp</p>
                                            <p class="text-sm font-bold text-white">{{ $message['card_data']['temperature'] }}</p>
                                        </div>
                                    </div>
                                </div>

                            @elseif($message['type'] === 'gallery')
                                <div class="bg-[#1F2029] border border-white/5 rounded-2xl rounded-tl-sm p-3 space-y-3 shadow-lg">
                                    <p class="text-sm text-slate-200 px-1">{{ $message['content'] }}</p>
                                    <div class="flex gap-2 overflow-hidden">
                                        @foreach($message['images'] as $index => $img)
                                            <div class="relative w-24 h-24 flex-shrink-0 rounded-lg overflow-hidden group/img">
                                                <img src="{{ $img }}" class="w-full h-full object-cover transition-transform duration-500 group-hover/img:scale-110">
                                                @if($index === 2)
                                                <div class="absolute inset-0 bg-black/60 backdrop-blur-[2px] flex items-center justify-center">
                                                    <span class="text-lg font-bold text-white">3+</span>
                                                </div>
                                                @endif
                                            </div>
                                        @endforeach
                                    </div>
                                </div>
                            @endif

                            <!-- Timestamp & Status -->
                            <div class="flex items-center gap-1.5 mt-1.5 {{ $message['sender'] === 'me' ? 'justify-end' : 'justify-start' }} opacity-0 group-hover:opacity-100 transition-opacity duration-300">
                                <p class="text-[10px] text-slate-500 font-medium">{{ $message['time'] }}</p>
                                @if($message['sender'] === 'me')
                                    <svg class="w-3 h-3 text-blue-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path></svg>
                                @endif
                            </div>
                        </div>

                        <!-- Avatar Right (Me) -->
                        @if($message['sender'] === 'me')
                        <img src="https://i.pravatar.cc/150?u=me" class="w-8 h-8 rounded-full ml-3 mt-1 border-2 border-[#0B0C10]">
                        @endif
                    </div>
                @endforeach
            </div>

            <!-- Input Area -->
            <div class="p-6 z-20">
                <form id="messageForm" class="bg-[#121217] border border-white/10 rounded-2xl p-2 flex items-center shadow-2xl relative">
                    @csrf
                    <!-- Attachment -->
                    <button type="button" class="p-2.5 rounded-xl text-slate-400 hover:bg-white/5 hover:text-white transition-colors">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.172 7l-6.586 6.586a2 2 0 102.828 2.828l6.414-6.586a4 4 0 00-5.656-5.656l-6.415 6.585a6 6 0 108.486 8.486L20.5 13"></path></svg>
                    </button>

                    <!-- Input -->
                    <input type="text" id="messageInput" name="message" placeholder="Write a message..." class="flex-1 px-4 py-2.5 bg-transparent focus:outline-none text-sm text-white placeholder-slate-500 font-medium" required>

                    <!-- Emoji/Send -->
                    <div class="flex items-center gap-2 pr-1">
                        <button type="button" class="p-2 rounded-xl text-slate-400 hover:bg-white/5 hover:text-white transition-colors">
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.828 14.828a4 4 0 01-5.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
                        </button>
                        <button type="submit" id="sendButton" class="w-10 h-10 bg-blue-600 hover:bg-blue-500 rounded-xl flex items-center justify-center text-white transition-all shadow-lg shadow-blue-600/20 hover:scale-105 active:scale-95">
                            <svg class="w-5 h-5 transform rotate-90 ml-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8"></path></svg>
                        </button>
                    </div>
                </form>
            </div>
        </div>

    </div>
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const messageForm = document.getElementById('messageForm');
    const messageInput = document.getElementById('messageInput');
    const messagesContainer = document.querySelector('.overflow-y-auto.p-8.space-y-6.pb-32');
    
    // Auto-scroll to bottom on load
    if (messagesContainer) {
        messagesContainer.scrollTop = messagesContainer.scrollHeight;
    }
    
    messageForm.addEventListener('submit', async function(e) {
        e.preventDefault();
        
        const message = messageInput.value.trim();
        if (!message) return;
        
        // Disable input while sending
        messageInput.disabled = true;
        
        try {
            const response = await fetch('{{ route("chat.send") }}', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                    'X-CSRF-TOKEN': document.querySelector('meta[name="csrf-token"]').content
                },
                body: JSON.stringify({ message: message })
            });
            
            const data = await response.json();
            
            if (data.success) {
                // Add message to UI
                const messageHtml = `
                    <div class="flex justify-end">
                        <div class="flex items-end gap-3 group">
                            <div>
                                <div class="px-5 py-3 rounded-2xl bg-gradient-to-br from-blue-600 to-blue-500 text-white shadow-lg shadow-blue-600/20 max-w-md">
                                    <p class="text-sm leading-relaxed">${escapeHtml(data.message.content)}</p>
                                </div>
                                <div class="flex items-center gap-1.5 mt-1.5 justify-end opacity-0 group-hover:opacity-100 transition-opacity duration-300">
                                    <p class="text-[10px] text-slate-500 font-medium">${data.message.time}</p>
                                    <svg class="w-3 h-3 text-blue-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path></svg>
                                </div>
                            </div>
                            <img src="https://i.pravatar.cc/150?u=me" class="w-8 h-8 rounded-full ml-3 mt-1 border-2 border-[#0B0C10]">
                        </div>
                    </div>
                `;
                
                messagesContainer.insertAdjacentHTML('beforeend', messageHtml);
                
                // Clear input
                messageInput.value = '';
                
                // Scroll to bottom
                messagesContainer.scrollTop = messagesContainer.scrollHeight;
            }
        } catch (error) {
            console.error('Error sending message:', error);
            alert('Failed to send message. Please try again.');
        } finally {
            messageInput.disabled = false;
            messageInput.focus();
        }
    });
    
    function escapeHtml(text) {
        const div = document.createElement('div');
        div.textContent = text;
        return div.innerHTML;
    }
});
</script>
@endsection
